-- For Github --

--make new directory (folder) >> mkdir 'dir name' >>"without quotes"
-- initialize git with (git init)
-- add new files (touch filename.extension
-- git add filename (or add .)
-- git commit -m 'pass comments' >> "with quotes"
-- create a repository on the github website
-- copy the url and connect to the repository with (git remote add 'unique name' 'url') >> "without quotes"
-- push your file/code from local to the repo with (git push -u 'unique name' master) "without quotes"
Hello
-- that was a wrong fomart, but moving on --